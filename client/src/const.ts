export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Placeholder function - public app requires no login
export const startLogin = () => {
  console.warn("Login disabled - app is public");
};
